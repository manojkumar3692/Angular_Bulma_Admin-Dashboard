import { DatePicketDirective } from './date-picket.directive';

describe('DatePicketDirective', () => {
  it('should create an instance', () => {
    const directive = new DatePicketDirective();
    expect(directive).toBeTruthy();
  });
});
